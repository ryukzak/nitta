import { RenderResult } from '@testing-library/react';
import {DebugScreen} from '../../screens/DebugScreen'

let documentBodyDebugScreen: RenderResult;

describe("<DebugScreen />", () => {
    test("test DebugScreen", async () => {});
  // beforeEach(() => {
  //   documentBodyDebugScreen = render(<DebugScreen />);
  // });

  // it('shows title', () => {
  //   expect(documentBodyDebugScreen.getByText('Current node:')).toBeInTheDocument();
  // });
});

