import { RenderResult } from '@testing-library/react';

import {ProcessScreen} from '../../screens/ProcessScreen'

let documentBodyProcessScreen: RenderResult;

describe("<ProcessScreen />", () => {
    test("test processScreen", async () => {});

  // beforeEach(() => {
  //   documentBodyProcessScreen = render(<ProcessScreen />);
  // });

  // it('shows title', () => {
  //   expect(documentBodyProcessScreen.getByText('Current node:')).toBeInTheDocument();
  // });
});

