import * as React from 'react';
import { RenderResult } from '@testing-library/react';
import { render } from "@testing-library/react";

import {NodeScreen} from '../../screens/NodeScreen'


let documentBodyNodeScreen: RenderResult;

describe("<NodeScreen />", () => {
    test("test nodeScreen", async () => {});
  // beforeEach(() => {
  //   documentBodyNodeScreen = render(<NodeScreen />);
  // });

  // it('shows title', () => {
  //   expect(documentBodyNodeScreen.getByText('Dataflow graph:')).toBeInTheDocument();
  // });

});



