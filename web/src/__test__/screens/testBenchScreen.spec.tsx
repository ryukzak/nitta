import { RenderResult } from '@testing-library/react';

import {TestBenchScreen} from '../../screens/TestBenchScreen'

let documentBodyTestBenchScreen: RenderResult;

describe("<TestBenchScreen />", () => {
    test("test TestBenchScreen", async () => {});
  // beforeEach(() => {
  //   documentBodyTestBenchScreen = render(<TestBenchScreen />);
  // });
  // it('shows title', () => {
  //   expect(documentBodyTestBenchScreen.getByText('Current node:')).toBeInTheDocument();
  // });
});



